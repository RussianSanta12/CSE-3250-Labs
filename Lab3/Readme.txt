in this lab I configured a RPI to use 10 gigs of storage and installed git on it to copy Block.py and testmine.py from the chainsthatbind us repo. I then copied the Rpi to make a second running on a different port. from that i tested rpi mine 1 and two to make sure that both rpi's ran on different ports then i ran two_rpi_mine and noticed that it wouldnt run both of them at the same time so i created 2thread_rpi_mine.py to use dual threading at the start by making each rpi a function at the start and running them side by side to see which one was faster because they were clones of each other i noticed that they would run within milisecconds of each other usually only differing by a couple cycles.
